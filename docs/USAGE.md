# Project Usage

## Using the Prompt File

To generate the initial `PRD.md` and `TASKS.md` for this project using the provided prompt, you can run Aider from your terminal and pass the `PROMPT.md` file as an argument. This will instruct the AI to follow the detailed steps outlined in the prompt file.

For example:
```bash
aider PROMPT.md
```
